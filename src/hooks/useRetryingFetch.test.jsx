describe('#useRetryingFetch', () => {
    describe('when making a request', () => {
        it('sets the loading state', () => {

        })

        describe('when the request is successful', () => {
            it ('clears any existing error data', () => {

            })

            it('provides the resulting data', () => {

            })
        })

        describe('when the request is unsuccessful', () => {
            it('retries the request a specified number of times', () => {

            })

            describe('when a subsequent request succeeds', () => {
                it('clears any existing error data', () => {

                })

                it('provides the resulting data', () => {

                })
            })

            describe('when all retries are unsuccessful', () => {
                it('clears existing data', () => {

                })

                it('provides the encountered error', () => {
                    
                })
            })
        })
    })
})